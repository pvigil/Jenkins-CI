properties([pipelineTriggers([githubPush()])])

node {
    stage ('Checkout'){
        git branch: 'master', url: 'https://github.com/pvigil/Jenkins-CI.git'
    }
    stage ('Build'){
        // steps
    }
    stage ('Test'){
        // steps
    }
}    
